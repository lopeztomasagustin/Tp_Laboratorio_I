#include <stdio.h>


 /** \brief Funcion para sumar dos enteros.
 * \param recibe un entero.
 * \param recibe un segundo entero.
 * \return devuelve resultado como un valor de tipo float.
 */

    float sumar (int numeroUno, int numeroDos);

 /** \brief Funcion para restar dos enteros.
 * \param recibe un entero.
 * \param recibe un segundo entero.
 * \return devuelve resultado como un valor de tipo float.
 */

    float restar (int restaUno, int restaDos);



 /** \brief Funcion para multiplicar dos enteros.
 * \param recibe un entero.
 * \param recibe un segundo entero.
 * \return devuelve resultado como un valor de tipo float.
 */

  float multiplicar(int multiplicacionUno, int multiplicacionDos);


    /** \brief Funcion para dividir dos numeros de tipo float.
    * \param recibe un primer valor de tipo float (El dividendo).
    * \param recibe un segundo valor de tipo float (El divisor).
    * \return devuelve resultado como un valor de tipo float.
    */

    float dividir(float divisionUno, float divisionDos);

     /** \brief Funcion calcular el factoriar de un valor de tipo int.
    * \param recibe un entero para realizar el factorial de dicho numero.
    * \return devuelve el factorial como un valor de tipo int.
    */


    float factorial(int numeroN);
